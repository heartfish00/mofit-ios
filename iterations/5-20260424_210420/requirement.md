# Requirement

## 가치제안

---
run_id: home-workout-newbie-20s_20260424_210609
persona_id: home-workout-newbie-20s
persona_version: 1
delivery_mode: landing_plus_meeting
created_at: 2026-04-24T21:06:09
---

# Mofit — iPhone 카메라만으로 스쿼트를 자동으로 세는 앱

## 1. 가치제안 요약 (랜딩페이지 카피)

### 메인 카피

**"폰 세워두고 손바닥 1초 또는 화면 탭. 스쿼트가 자동으로 세어집니다."**

iPhone 카메라만으로 스쿼트를 자동으로 세고, 관절 각도로 실시간 자세를 보여줍니다. 별도 장비·회원가입 없이, 앱 켠 지 1분 안에 첫 카운트가 올라갑니다.

### 서브 카피

- **스쿼트 전용 자동 카운터**: Apple Vision으로 hip–knee–ankle 각도 분석. 서있음(>160°) → 앉음(<100°) → 서있음 = 1 rep 인정. 분석 15fps 샘플링(배터리 절약 + 스쿼트 속도에 충분).
- **로그인 없이 바로 시작**: 설치 → 온보딩 → 즉시 카운트. 기록은 내 폰 로컬(SwiftData)에만 저장, 외부 전송 0.
- **시작·종료 트리거 이중화**: 손바닥 1초 OR **화면 탭**. 손바닥 인식이 안 되는 각도/조도에서도 탭 한 번으로 카운트다운 시작.
- **AI 코칭 샘플은 로그인 전에도 미리 본다**: 코칭 탭에 들어가면 로그인 전에도 샘플 피드백 2종이 먼저 보여서, 로그인할 가치가 있는지 확인 후 결정할 수 있음.
- **광고 없음 · 결제 없음**: MVP 단계는 전면 무료. 기본 카운팅·기록·자세 오버레이는 영구 무료 유지 계획.

### 타깃 시나리오

> "헬스장 3개월 등록했는데 2주 못 갔어요. PT는 회당 7~10만원, 유튜브 홈트는 내 폼이 맞는지 모르겠어요."

Mofit은 바로 이 지점을 겨냥합니다. 폰을 벽에 기대 세우고 손바닥을 카메라에 1초 보여주거나 화면을 탭하면 5초 카운트다운 후 추적이 시작됩니다. 스쿼트가 끝날 때까지 폰을 만지지 않아도 되고, 끝낼 때도 탭 또는 손바닥으로 세트 종료가 가능합니다.

### 현재 지원 범위 (솔직한 고지)

- **Mofit은 스쿼트 전용 카운터입니다.** 푸쉬업·런지·싯업·플랭크 등 다른 운동은 제공하지 않습니다.
- 홈 탭 시작 버튼은 "스쿼트 시작" 하나뿐이며, 운동 종류 선택 UI 자체가 없습니다.
- 다른 운동 확장에 대한 일정은 약속하지 않습니다. 지금 설치하면 **스쿼트를 자동으로 세는 앱**으로 쓰게 됩니다.

### 솔직한 성능 고지

- 내부 개발 중 관측한 감각으로는 권장 환경(밝은 실내, 전신이 프레임에 들어오는 거리 2~3m, 일상복)에서 대체로 안정적으로 카운트됩니다. **다만 공개 가능한 정량 정확도 수치(예: 오카운트율 %)는 현재 확보되어 있지 않습니다.** 베타 이후 실제 사용 데이터로 측정해 구체 수치로 갱신할 계획.
- 조도 낮음 / 카메라 너무 가까움 / 하체 일부가 프레임 밖 등의 조건에서는 카운트가 튀거나 멈추는 경우가 있습니다 — 현 버전은 "왜 안 되는지"를 안내하는 UI가 아직 얕습니다 (§5 참고).

## 2. 구체적 기능 설명 (미팅 / 데모 전달 시)

### 2.1 온보딩 (최초 1회, 약 3~4분)

성별 → 키 → 몸무게 → 체형(마른/보통/근육질/통통한) → 목표(체중 감량/근력 증가/체형 개선) → 코치 스타일. 총 6단계, 입력값 허용/경고 범위 검증 포함. 회원가입·이메일 요구 없음. 입력은 전부 폰 로컬(SwiftData)에 저장.

### 2.2 홈 탭

- 좌상단: "Mofit" 타이틀. 우상단: 프로필 편집 버튼.
- **"스쿼트 시작" 단일 버튼(형광초록)**. 탭 시 즉시 트래킹 화면 진입. 운동 종류 선택 UI 없음 (ADR-017).
- 오늘의 기록 요약: "세트 N · 스쿼트 N회 · N분" (기록 없으면 "첫 운동을 시작해보세요!")
- 운동 종료 후 복귀 시 폭죽 효과.

### 2.3 트래킹 화면 (카메라)

- **스쿼트 판정**: `VNDetectHumanBodyPoseRequest`로 hip-knee-ankle 각도 분석. 왼/오 다리 중 하나 기준. 서있음(>160°) → 앉음(<100°) → 서있음 = 1 rep.
- **손바닥 판정**: `VNDetectHumanHandPoseRequest`로 5개 손가락이 1초 펴진 상태 감지 → 세트 시작/종료 신호.
- **화면 탭 대체 트리거**: idle/tracking 상태에서 ZStack 전체 `.onTapGesture` 활성. 손바닥이 안 잡히는 각도·조도에서도 탭 한 번으로 동일 경로 실행. idle 서브카피: "손바닥을 보여주거나 화면을 탭하세요". tracking 서브카피: "끝낼 땐 화면을 탭하거나 손바닥을 보여주세요".
- 관절 포인트가 화면에 오버레이로 표시.
- 프리뷰 30fps, 분석 15fps 샘플링.

### 2.4 기록 탭

- 상단: 최근 7일 가로 스크롤 날짜바.
- 선택 날짜의 세션 리스트 (세트 수, 총 rep, 시간, 시작 시각).
- 스와이프 삭제.
- 기록 없는 날: "이 날은 운동 기록이 없어요".

### 2.5 AI 코칭 탭

**비로그인 분기 (설치 직후 바로 확인 가능)**: 로그인하지 않아도 코칭 탭에 진입하면 "로그인하면 이런 피드백을 받습니다" 섹션과 함께 **샘플 피드백 카드 2종**이 먼저 노출됩니다. 각 샘플은 **숫자 + 요일/시점 + 구체 행동 제안** 3요소를 포함:

- **샘플 A (운동 전 동기부여)**: *"지난 주 3일 운동 · 총 78회 스쿼트 · 일평균 26회. 수요일엔 38회 최다였고 금요일 0회였네요. 오늘은 수요일 페이스 회복해서 30회 이상 도전해보세요."*
- **샘플 B (운동 후 피드백)**: *"오늘 3세트 총 32회. 1세트 14회 → 2세트 10회 → 3세트 8회. 세트별 감소폭 4회로 피로도 자연스러운 곡선. 내일은 2세트째에서 쉬는 시간 20초 늘려보세요."*
- 각 카드 하단에 "※ 예시 피드백 (실제 데이터 기반으로 매번 다름)" 고지.

**로그인 분기 (활성)**: 하루 2회 호출(운동 전 동기부여 / 운동 후 피드백, 자정 리셋). **앱 → 자체 서버 `/coaching/request` → Anthropic Claude API** 경유 (API 키는 앱 바이너리에 없음). 전달 컨텍스트: 사용자 프로필 + 최근 7일 요약(운동일수, 총 세션, 총 rep, 일평균 rep) + 일별 추이(rep/세트/세트당 평균). API 실패 시 일일 횟수 차감 안 함. 서버는 DB 유일성 제약으로 1일 2회를 강제.

### 2.6 인증 (선택)

- 이메일 + 비밀번호 가입/로그인 (Supabase + JWT, Keychain 저장).
- 로그인 유저: 프로필/운동 기록/AI 피드백이 서버(Supabase)에도 저장.
- 비로그인 유저: 모든 데이터 로컬(SwiftData)에만 저장, 외부 전송 0.
- **주의**: 비로그인 상태로 쌓은 로컬 기록은 로그인 후 서버로 **이관되지 않음** (의도된 설계, ADR-013).

## 3. 가격 / 무료 범위 경계 (솔직한 고지)

### 단계 0 (현재, MVP)

- 전 기능 무료, 광고 없음.

### 단계 1 가설 (수개월 후, 미확정)

- **영구 무료로 유지하려는 기능**: 스쿼트 자동 카운팅, 세션 기록 저장/조회, 관절 각도 오버레이.
- **과금 후보 영역**: 하루 2회를 초과하는 AI 코칭 호출, 주간 리포트·장기 추이 분석, 커스텀 자세 가이드 — **아직 확정된 플랜 아님**.

## 4. 프라이버시 / 데이터

- 카메라 영상은 **어디에도 저장·전송되지 않음**. 온디바이스 Vision 분석 후 즉시 폐기.
- 비로그인: 모든 데이터 로컬. 로그인: Supabase 서버 저장 + Claude API 서버 프록시.
- API 키는 앱 바이너리에 없음 (서버 프록시).

## 5. 리스크 · 한계 · 아직 얕은 부분 (선제 고지)

현재 MVP 단계에서 **의도적으로 얕게 남긴 영역**입니다. 이 지점들은 "지금은 몰래 잘 되는 척" 하지 않고 미리 고지합니다.

1. **rep 카운트 정량 정확도 공개 미제공** — 권장 환경/비권장 환경에서의 수치(오카운트율 %, 미검출률) 확보 전. 베타 이후 측정해 갱신. 내부적으로도 실패 케이스 로깅 파이프라인이 아직 없음.
2. **실패 가이드 UI 얕음** — "카메라에 전신이 안 들어왔다" / "조도가 낮다" / "거리가 너무 가깝다" 같은 실패 원인 안내 UI는 현재 없음. 트래킹 중 카운트가 안 올라가면 사용자가 혼자 원인을 추정해야 함.
3. **스쿼트 외 운동은 영구적으로 제공되지 않을 수 있음** — MVP는 스쿼트 전용 포지셔닝이 확정됨(ADR-017). "홈트 앱"을 기대하고 설치하면 어긋남.
4. **앱스토어 출시 전 단계** — 별점·리얼 리뷰 등의 외부 신뢰 신호가 아직 없습니다. TestFlight 베타 배포도 준비 중. README에 스크린샷도 아직 없음. 현재 시점에서 공개 가능한 신뢰 신호는: (a) 이 문서 자체의 범위/한계 투명 고지, (b) 설치·사용 **비용이 0**이라 "망설이지 않고 설치해 직접 확인" 가능, (c) 카메라 영상 비저장/비전송 + API 키 서버 분리 등 프라이버시 설계 근거.
5. **비로그인 → 로그인 로컬 기록 미이관 (ADR-013)** — 비로그인 상태에서 쌓은 기록은 로그인해도 서버로 옮겨지지 않음. "3일 무료로 써서 쌓인 기록이 로그인 시 통째로 리셋되는 것처럼 보일 수 있다"는 유저 불신 이슈가 아직 남아 있음.
6. **크래시/데이터 복구 없음** — 앱 크래시 시 진행 중 세트는 복구되지 않음 (MVP 스코프 외).

## 6. 설치 후 첫 10분 시나리오 (기대치 교정)

1. 설치 → 카메라 권한 허용 → 온보딩 3~4분
2. 홈 탭에서 "스쿼트 시작" 버튼 탭 → 카메라 뷰 진입
3. 폰을 벽에 기대거나 거치 → 전신이 프레임에 들어오도록 2~3m 거리
4. 손바닥을 카메라 방향으로 1초 들거나 **화면을 탭** → 5초 카운트다운 → 추적 시작
5. 스쿼트 수행 → rep이 올라가는 것을 확인
6. 끝낼 때 다시 손바닥 1초 또는 **화면 탭** → 세트 종료, 결과 화면
7. (선택) 코칭 탭에서 샘플 피드백 2종 확인 후 로그인 여부 결정

## 채택된 요구사항

- **run_id**: `home-workout-newbie-20s_20260424_210609`
- **title**: 트래킹 중 미검출 상태에서 "왜 안 세어지나요?" 진단 힌트 오버레이 — `.outOfFrame` / `.lowLight` 2종 고정 케이스

### 유래한 고객 pain + 근거 인용

**페르소나** (`persuasion-data/personas/home-workout-newbie-20s/profile.md`):

- `reject_triggers[2]`: "실제로 써보니 rep 카운트가 자주 틀림 → 신뢰도 붕괴"
- `personality_notes`: "3일 써보고 아니면 삭제"
- `tech_literacy: medium` — "왜 안 되는지" 혼자 추정하기 부담스러운 수준

**시뮬 결과** (`run_id: home-workout-newbie-20s_20260424_210609`, keyman `decision: drop`, `confidence: 55`, 최종 판정 **실패**):

> **`01_keyman_initial.md` 라인 20 (구체적 이유 #4, 프로덕트 성숙도 증거 부재)**:
> "가치제안 §5-2에서 '실패 가이드 UI 얕음'을 스스로 고지했으므로, medium 수준의 내 tech literacy로도 '왜 안 되는지'를 혼자 추정해야 하는 부담이 남는다. 신뢰도 감점 사유."

> **`01_keyman_initial.md` 라인 27 (걱정/의문점 #1)**:
> "권장 환경(밝은 실내·2~3m·전신) 벗어나면 카운트가 얼마나 튀는가? 내 방은 조명이 그리 밝지 않고, 2~3m 거리 확보도 애매하다."

> **`report.md` 라인 48 (가치제안 개선 포인트 #7, 엣지 케이스·복구 시나리오 공백)**:
> "조명·거리 민감도 자료, 백그라운드 복귀 시 세트 유지 설계가 보수적 소비자 설득 재료."

이 페르소나는 "3일 써보고 아니면 삭제" 패턴이 명시되어 있어, 첫 3일 안에 카운트 실패를 한 번이라도 경험하면 원인 추정이 안 돼서 즉시 이탈. 현 버전은 미검출 상태에 대한 인앱 가이드가 전혀 없고, Mofit 스스로도 가치제안 §5.2에서 "실패 가이드 UI 얕음"을 자체 고지한 한계. CTO는 이를 "문서의 자기고지는 신뢰 얻지 못하고 인앱에서 증명돼야 하는 pain"으로 승인 근거에 명시.

### 구현 스케치

운동 추적(tracking) 상태에서 **최근 3초간 hip/knee/ankle 중 한쪽(left 또는 right)의 세 조인트가 모두 검출되지 않은 채로 rep 카운트가 0**인 상태가 유지되면, 카메라 프리뷰 상단에 가벼운 힌트 카드 노출.

**UX 규칙**:

- 트래킹 시작 후 최소 5초 유예 (카메라 안정화 시간)
- 원인별 우선순위:
  1. 한쪽 다리 전체 조인트 미검출 → "전신이 프레임에 들어오는지 확인하세요 (2~3m 거리 권장)"
  2. 조인트 검출은 되지만 평균 confidence가 임계치 이하 → "조명이 어두울 수 있어요 · 실내 조명을 밝혀주세요"
- 한 세션 내 rep이 한 번이라도 카운트되면 힌트 숨김 + 이후 재표시 금지 (사용자 주의 분산 방지)
- 힌트는 blocking 아닌 반투명 배너 형태. 하단 종료 버튼 시야 가리지 않도록 상단.

**구현 파일**:

1. **`Mofit/Services/PoseDetectionService.swift`**: 기존 `detectPose(in:)` 시그니처 유지한 채로 avgConfidence / missingCriticalJoints 를 함께 반환하는 부가 메서드(또는 optional 필드 구조체) 추가. 기존 호출부 전면 리팩터링 금지.
2. **`Mofit/ViewModels/TrackingViewModel.swift`**: `@Published var diagnosticHint: DiagnosticHint?` 추가. enum 케이스는 `.outOfFrame`, `.lowLight` 2종 고정. "미래 확장"을 위한 third case / 프로토콜 / 전략 패턴 금지. 유예 시간/임계치 판정 로직 추가.
3. **`Mofit/Views/Tracking/TrackingView.swift`**: 상단에 `if let hint = vm.diagnosticHint { HintBanner(hint: hint) }` 오버레이 추가.
4. **`HintBanner.swift` (새 파일) 또는 TrackingView 내부 subview**: 반투명 배경 + SF Symbol + 카피 1줄. 고정 스트링 2종.
5. **상수 한 곳에 모으기**: 힌트 카피 2종, 임계치(5초 초기 grace, 3초 미검출 유지, confidence 평균 임계치)은 하드코딩 상수로 한 파일(예: TrackingViewModel 내 private enum `Diagnostic`)에 모아둘 것. ConfigService 신규 레이어 금지.

**인간 개입 지점**:

- 실기기 QA 1회 (CTO 조건부 5): (a) 정상 환경 스쿼트 3rep 여전히 카운트되는지, (b) 일부러 프레임 밖으로 나가면 3~5초 내 `.outOfFrame` 힌트가 뜨는지. 두 케이스만 통과하면 merge 가능.
- 조도 케이스는 실기기 검증 생략 가능 (임계치 로직만 단위 테스트).

### CTO 승인 조건부 조건

CTO 판정: **승인 / confidence 72**. 다음 5개 조건 이행 필수:

1. **힌트 카피·임계치는 한 파일에 하드코딩 상수로 모을 것.** ConfigService 같은 설정 레이어를 새로 만들지 말 것 — 이 스프린트의 scope가 아님.
2. **DiagnosticHint enum은 2종(.outOfFrame, .lowLight)만.** "미래 확장"을 위한 third case / 프로토콜 / 전략 패턴 도입 금지. 추가 케이스는 다음 실제 고객 근거가 나온 뒤.
3. **`PoseDetectionService` 반환 구조 확장 시 기존 `detectPose(in:)` 시그니처 유지.** 부가 메서드 하나 추가하거나 optional 필드 구조체로만 변경. 호출부 전면 리팩터링 금지.
4. **"한 세션 내 rep 한 번 카운트되면 이후 숨김" 규칙 반드시 구현.** 세션 내 재표시 토글 로직 추가 금지 — 사용자 주의 분산 유발 위험.
5. **실기기 QA는 merge 전 최소 1회**: (a) 정상 환경 스쿼트 3rep이 여전히 카운트되는지, (b) 일부러 프레임 밖으로 나가면 3~5초 내 `.outOfFrame` 힌트가 뜨는지. 이 두 케이스만 통과하면 됨. 조도 케이스는 실기기 검증 생략 가능(임계치 로직만 단위 테스트).
